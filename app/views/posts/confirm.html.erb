<h3>以下の内容でつぶやきますか？</h3>

<%= form_with(model: @post, url: posts_path, local: true) do |form| %>
  <%= form.hidden_field :content %>

  <p>内容: <%= @post.content %></p>
  <%= form.submit "つぶやく" %>

  <%= form.submit "修正する", name: "back" %>
<% end %>